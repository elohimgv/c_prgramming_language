// This first line tells the compiler to include information 
// about the standard library
#include <stdio.h>

// The program begins executing at the beggining
// of main. This means that every program (in C)
// must have a 'main' somewhere. 
int main() 
{
    printf("Hello, world\n");
}

// A function contains 'statements' that specify
// the computing operations to be done, and 
// variables store values used during the computation 